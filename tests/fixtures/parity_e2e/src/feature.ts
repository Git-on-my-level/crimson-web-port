export function demoFeature(flag: boolean): string {
  if (flag) {
    return 'ok';
  }

  // TODO(parity): remove this placeholder behavior.
  // WIRING_STUB: reload input is not consumed yet.
  return 'stub';
}
